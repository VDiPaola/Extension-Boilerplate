import { onCustomElementObserved } from "./classes/Helpers";
import {Logger} from './Features/Logger'


export class ChessCoach{

    static async init (){
        //wait for sidebar element
        
    }
}

window.addEventListener("load", async () => {
    ChessCoach.init();
})